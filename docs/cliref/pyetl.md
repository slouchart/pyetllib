# `pyetl` — Command Line Interface

```vim
Usage: pyetl show plugins
       pyetl run [global options] <ETL plugin> [plugin options]
```

## subcommand `show plugins`
Displays installed ETL plugins
 
## subcommand `run`
Runs an ETL job as a plugin
